# Infrastructure

Local and production Compose, Caddy, deployment, backup, and restore assets will be added by their acceptance PRs. Production design must remain compatible with the 2GB VPS limits in `DEV_SPEC.md`.

