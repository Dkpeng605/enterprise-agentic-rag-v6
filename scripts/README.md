# Scripts

Repository-level developer and operational helpers belong here. Scripts must call the same backend application services as the API and MCP entrypoints rather than reimplementing business logic.

